<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalle Pago - GymApp</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Detalle del Pago</h1>
            <nav>
                <a href="/" class="btn">Inicio</a>
                <a href="/pagos" class="btn">Volver a Pagos</a>
                <a href="/pagos/<%= pago.id %>/editar" class="btn btn-warning">Editar</a>
            </nav>
        </header>

        <main>
            <div class="detail-container">
                <div class="card">
                    <div class="card-header">
                        <h2>$<%= parseFloat(pago.monto).toFixed(2) %></h2>
                        <span class="method <%= pago.metodoPago.toLowerCase() %>">
                            <%= pago.metodoPago %>
                        </span>
                    </div>
                    <div class="card-body">
                        <p><strong>Cliente:</strong> <%= pago.clienteNombre %></p>
                        <p><strong>Fecha de Pago:</strong> <%= new Date(pago.fechaPago).toLocaleDateString('es-ES') %></p>
                        <p><strong>Método de Pago:</strong> <%= pago.metodoPago %></p>
                        <p><strong>Número de Confirmación:</strong> <%= pago.numero_confirmacion %></p>
                        <p><strong>ID:</strong> <%= pago.id %></p>
                    </div>
                </div>
            </div>
        </main>
    </div>
</body>
</html> 